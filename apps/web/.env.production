# Astro 는 PUBLIC_* 를 빌드 시점에 코드에 그대로 박아 넣는다.
# 따라서 운영 값은 wrangler.jsonc 의 vars 가 아니라 여기(또는 CI 환경변수)에서 와야 한다.
# 전부 브라우저에 노출되는 값이므로 비밀이 아니다.
PUBLIC_SITE_URL=https://www.namsu.kim
PUBLIC_API_URL=https://api.namsu.kim
PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAFBQAkl355PecYGw

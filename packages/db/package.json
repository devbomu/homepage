{
  "name": "@namsu/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate:local": "wrangler d1 migrations apply namsu-kim --local",
    "migrate:remote": "wrangler d1 migrations apply namsu-kim --remote",
    "seed:local": "wrangler d1 execute namsu-kim --local --file=./seed.sql",
    "reset:local": "rm -rf .wrangler && pnpm migrate:local && pnpm seed:local",
    "studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260922.1",
    "drizzle-kit": "^0.31.11",
    "typescript": "^5.9.3",
    "wrangler": "^4.136.2"
  }
}
